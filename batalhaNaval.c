#include <stdio.h>

#Define SIZE 10 // Tamanho do tabuleiro
#Define CONE_SIZE 5 //  Define Tamanho da matriz de habilidade Cone
#Define CROSS_SIZE 5 // Define Tamanho da habilidade Cruz
#Define OCTAHEDRON_SIZE 5 // Define Tamanho da habilidade Octaedro

// Função para innicializar o tabuleiro com água (0)
void initializeBoard (int board[SIZE] [SIZE]){
    for (int i = 0; i< SIZE; i++){
        for (int j = 0; j < SIZE; j++){
            board [i][i = 0; // 0 representa água]
        }
    }
} 

// Função para exibir Tabuleiro
void displayBoard ((int board [SIZE] [SIZE]){
    for (int i =  0; )
}